import Login from '@/components/Login'
import React from 'react'

const page = () => {
  return (
    <div>
      <Login/>
    </div>
  )
}

export default page

export const metadata={
    title:'Login - ChaiCharm!'
}